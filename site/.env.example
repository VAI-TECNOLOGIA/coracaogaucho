# Banco PostgreSQL (Railway em produção)
# Ex.: postgresql://postgres:SENHA@HOST.proxy.rlwy.net:PORTA/railway
DATABASE_URL=""

# Segredo de assinatura dos JWTs de sessão (obrigatório em produção)
# Gere com: openssl rand -hex 32
AUTH_SECRET=""

# Opcional — habilita o resumo executivo com IA no módulo Insights
ANTHROPIC_API_KEY=""

# Opcionais — analytics (carregadas só se definidas)
NEXT_PUBLIC_GA_ID=""
NEXT_PUBLIC_GTM_ID=""
NEXT_PUBLIC_META_PIXEL_ID=""
